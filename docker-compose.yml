services:
  backend:
    build: ./backend
    volumes:
      - db_data:/app/db
    restart: unless-stopped

  frontend:
    build: ./frontend
    expose:
      - "80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  db_data:
